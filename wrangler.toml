name = "riptar"
main = "build/index.js"
compatibility_date = "2025-02-11"

[[routes]]
pattern = "riptar.gregermendle.com"
custom_domain = true

[build]
command = "cargo install -q worker-build && worker-build --release"

[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = true
